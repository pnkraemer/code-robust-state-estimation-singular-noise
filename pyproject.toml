[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
dynamic = ["version"]
name = "ckf"
dependencies = [
    "jax[cpu]",
    "pytest",
    "pytest_cases"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["ckf"]